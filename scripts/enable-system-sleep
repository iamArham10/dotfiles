#!/usr/bin/env bash
set -euo pipefail
DST="/etc/systemd/sleep.conf.d/disable-sleep.conf"
[[ -f "$DST" ]] || { echo "Nothing to remove: $DST" >&2; exit 1; }
sudo rm "$DST"
echo "Removed $DST (suspend/hibernate allowed again)."
